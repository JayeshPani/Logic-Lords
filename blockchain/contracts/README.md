# Smart Contracts

Contains solidity contracts for maintenance evidence anchoring and verification.

## Deployment

Deploy `InfraGuardVerification.sol` to Sepolia using:

```bash
bash blockchain/scripts/deploy_sepolia_foundry.sh
```

This project intentionally avoids Hardhat for deployment.
